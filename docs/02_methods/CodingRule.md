# コーディング・ドキュメント記法規約（研究プロジェクト用）

**最終更新**: 2026-07-18  
**関連ドキュメント**:  

- AI指示書 → [CLAUDE.md](../../CLAUDE.md)
- 実装例 → [src/gee/gee_calc_LST.py](../../src/gee/gee_calc_LST.py)
- ドキュメント全体 → [docs/README.md](../README.md)

**適用範囲**: プロジェクト内のすべてのPythonスクリプト（1〜13章）、すべてのMarkdownドキュメント（14章）、AIエージェントによるツール利用全般（16章）

---

本ドキュメントは、研究プロジェクトにおけるコードとドキュメントの品質・一貫性を確保するための規約です。Pythonコードは PEP 8 に準拠し、Markdownドキュメントは markdownlint によるルールに従います。

`ruff`（`ruff check` + `ruff format`）および `markdownlint` による自動検査を導入しているが、自動で検出できない項目（日本語コメント・docstringの必須化、日本語変数名の禁止、ファイルヘッダー形式など）も含むため、コミット前のセルフレビューは引き続き必要（[13. ツールによる自動チェックの範囲](#13-ツールによる自動チェックの範囲)を参照）。

---

## 1. 命名規則

- **変数名・関数名**：英語で意味のある名前を付ける（例：`calculate_mean`、`data_frame`）。日本語変数名は使用しない。
- **クラス名**：単語の先頭を大文字にしたキャメルケース（例：`DataAnalyzer`）。
- **定数**：全て大文字、単語間はアンダースコア（例：`MAX_COUNT`）。

## 2. インデント・空白

- インデントは**半角スペース4つ**。
- タブは使用しない。
- 演算子の前後、カンマの後にはスペースを入れる。
- 上記は `ruff format` により自動整形される。

## 3. 型ヒント

- 関数の**引数・戻り値には原則として型ヒントを付与**する。
- 標準的な型は `pathlib.Path`、`list[str]`、`dict[str, Any]` などPEP 585以降の組み込みジェネリクスを使用する。
- 引数・戻り値のないシンプルな`main()`関数にも `-> None` を付与する。

## 4. コメントの書き方

- **日本語で記述**し、簡潔かつ分かりやすく。
- 初心者にも理解できるように丁寧に説明する。
- コードの意図や処理内容、注意点を明記する。
- 関数やクラスには**ドキュメンテーションストリング（docstring）**を必ず付与。

### コメント例

```python
# データフレームから欠損値を除外する
cleaned_df = df.dropna()
```

### 4.1 Issue番号の記載禁止（コード・ドキュメント共通）

コードやドキュメントに書かれたIssue番号をAIが読み取ると、参照先のIssueへアクセスしに行き、無駄にコンテキストを消費する。このため、以下の箇所にはIssue番号（`#123` 等）を記載しない。

- **対象（記載禁止）**: docstring・コード内コメント・`docs/` 配下のドキュメント本文
- **例外（記載してよい・必要な箇所）**: コミットメッセージ・PRタイトル（末尾の `(#Issue番号)` 形式）、PR本文の `Closes #番号`、Issue本文の「関連 Issue」欄など、GitHub上の運用でIssue番号による紐づけが必要な箇所

経緯や背景を残したい場合は、Issue番号ではなく内容そのもの（何を・なぜ）を書く。詳細な経緯はコミットメッセージ・PR・Issueから追跡できる。

## 5. 関数・クラス設計

- 1関数1責務を意識し、処理を分割する。
- 関数には必ず**docstring**を付け、引数・戻り値・処理内容を日本語で記述（Googleスタイル準拠）。
- クラスも同様にdocstringを付与。
- **引数を持たない関数**では、docstringの`Args`セクションを省略する（「Args: なし」のような記述はしない）。

### docstring例

```python
def calculate_mean(values: list[float]) -> float:
    """
    平均値を計算する関数

    Args:
        values: 計算対象の数値リスト
    Returns:
        平均値
    """
    return sum(values) / len(values)
```

### 5.1 重複コードの共通化

- 同一・ほぼ同一の処理が**2つ目のファイルで必要になった時点**で共通モジュール `src/common/` へ抽出する（Rule of Two）。1回しか使わない処理を事前に共通化しない（過度な抽象化の禁止）
- 共通化の対象は関数だけでなく**定数**（CRS・デフォルトパス・プロジェクトルート等）も含む
- 共通モジュールの配置場所は `src/common/`、対応するテストは `tests/common/` に**必須**（[11. テスト方針](#11-テスト方針)に従う）
- import は `from src.common.{モジュール} import ...` の絶対importに統一する（`__init__.py` での再エクスポートはしない。利用側は必要なサブモジュールだけをimportし、不要な依存伝播を防ぐ）
- 実行は `python -m src.{パッケージ}.{スクリプト名}` 形式（[docs/setup.md](../setup.md) 6章参照）

## 6. ファイルヘッダー

- ファイル先頭は**module docstring**のみとし、概要・用途・依存関係などを日本語で記述する。
- `# 作成者: ...` `# 作成日: ...` のような行コメントは付与しない（作成者・作成日は `git log` で確認できるため）。

## 7. パスの扱い・再現性

- 入出力パスは**相対パス**・`pathlib.Path` を用いる。
- **研究データの絶対パス（ユーザー名やドライブ直下の研究データディレクトリなど）はハードコードしない**。
- 例外として、**外部ツールの実行ファイル探索パス**（QGIS同梱の`ogr2ogr.exe`など、OS環境に依存するインストール先）は、フォールバック候補のリストとして絶対パスを記述してよい。
- 乱数を使用する処理では、必ずシードを設定する。

## 8. コーディングの基本方針

- 情報の**正確性**と**一貫性**を重視する。
- 再現性のあるコードを書く。
- 例外処理（try-except）を適切に行う。

## 9. エラー処理

- 予期しないエラーに備え、適切な例外処理を実装する。
- エラーメッセージは日本語で分かりやすく記述する。

## 10. 使用ライブラリ

現在`environment.yml`に含まれる主なライブラリ（2026-06時点）：

- **データ分析**：NumPy、Pandas、Matplotlib
- **地理空間データ処理**：Geopandas、Shapely、Fiona、Rasterio、PyProj
- **衛星画像解析**：Google Earth Engine API（earthengine-api）
- **機械学習・統計**：Scikit-learn、SHAP

これは現状の一覧であり、ライブラリ選定を制限するものではない。実装内容に応じて最適なライブラリを選定してよい。`environment.yml`に含まれないライブラリが必要な場合は、追加前にユーザーの承認を得てから`environment.yml`に追加する。

`environment.yml` を環境構築の正本とし、手順は [../setup.md](../setup.md) に記載する。  
`requirements.txt` は参照用であり、環境構築の起点にはしない。

## 11. テスト方針

Python ファイルの新規作成・機能変更時には、対応するテストコードの作成を**必須**とする。

### 11.1 基本ルール

- テストフレームワークは `pytest` を使用する
- テストファイルは `tests/` 配下に `test_*.py` として配置し、ソースコードのディレクトリ構造に対応させる
- テスト実行コマンド: `pytest tests/` （プロジェクトルートから実行）
- 全ての処理を網羅するテストは必須としないが、新規作成・変更した関数には最低限のテストを用意する

### 11.2 テストの種類と使い分け

対象に応じて適切な粒度のテストを選択する。

| テストの種類 | 目的 | 適用場面 |
|---|---|---|
| **ユニットテスト** | 個々の関数の入出力が期待通りか検証 | 計算ロジック、データ変換、ユーティリティ関数 |
| **スモークテスト** | 処理が正常終了し、期待する出力形式（レイヤー・カラム・CRS等）を持つか確認 | GIS処理スクリプト、パイプライン処理 |
| **スキーマ検査** | 出力するCSV/JSON/GeoDataFrameが想定するカラム名・キー・型を持つか確認 | データ出力処理 |

### 11.3 テスト作成の指針

- テストデータは小規模なサンプルデータを使用し、`conftest.py` のフィクスチャで管理する
- 外部サービス（Google Earth Engine等）に依存する処理は、テスト可能な部分を分離してテストする
- 回帰を防止したい主要な処理には、境界値やエラーケースのテストも追加する

### 11.4 テスト必須の適用範囲

すべての Python ファイルに一律でテストを課すのではなく、配置ディレクトリで機械的に判定する。

| 配置 | テスト | 位置づけ |
|---|---|---|
| `src/` 配下 | **必須** | 再利用スクリプト（分析パイプライン・データ取得・共通処理） |
| `sandbox/` 配下 | 不要 | 探索・単発スクリプト（使い捨て前提。Git 追跡外） |

- 探索的な試行は `sandbox/` で行い、再利用する価値が確認できた時点で `src/` へ移動（昇格）する。**昇格の時点でテストを作成し、本規約のすべての項目を適用する**
- `sandbox/` 配下でも、命名規則・日本語コメントなどの基本規約には従うことを推奨する（昇格時の手戻りを減らすため）

## 12. その他

- コードは定期的に見直し、リファクタリングを行う。
- 不要なコードや使われていない変数は削除する。

## 13. ツールによる自動チェックの範囲

`ruff`・`markdownlint` で機械的に保証される項目と、セルフレビューで確認する項目を区別する。

`ruff`・`pytest`・`markdownlint` は [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) により
PR作成時・`main` へのpush時にも自動実行される（[setup.md 12章](../setup.md#12-cigithub-actions)参照）。
ローカルでの実行（pre-commit・エディタ拡張）に加えた二重のゲートであり、テストが通らないコードが
`main` に入ることを防ぐ。

### ruffで保証される項目（Python）

- PEP 8準拠（インデント・空白・行長など）
- import順序（isort相当）
- 未使用のimport・変数
- `ruff format` によるフォーマット統一

### markdownlintで保証される項目（Markdown）

- コードフェンスの言語指定（MD040）
- 見出しレベルの連続性（MD001）
- 記法の統一（コードフェンス記号・強調・太字のスタイル）
- 空白・改行の整合性（末尾スペース、連続空行、タブ禁止など）

### セルフレビューで確認する項目（自動検出不可）

- コメント・docstringの日本語記述、必須箇所への付与
- Issue番号の不記載（コメント・docstring・docs本文。[4.1](#41-issue番号の記載禁止コードドキュメント共通)参照）
- 日本語変数名の禁止
- ファイルヘッダー形式（module docstringへの一本化）
- 絶対パスの扱い（研究データパス禁止／外部ツール探索パスの許容）
- 再現性（乱数シード、相対パス）

## 14. ドキュメント記法ルール（Markdown）

`markdownlint`（VS Code 拡張 + pre-commit フック）による自動チェックを導入している。ルール設定は `.markdownlint.json` に定義されている。

### コードフェンスの言語指定（MD040）

コードフェンス（` ``` `）には**必ず言語名を指定**する。言語指定がないとレビューツール（CodeRabbit 等）で警告される。

よく使う言語名:

| 言語名 | 用途 |
|---|---|
| `python` | Pythonコード |
| `bash` | シェルコマンド（Linux/macOS） |
| `powershell` | PowerShellコマンド（Windows） |
| `json` / `jsonc` | JSON / コメント付きJSON |
| `yaml` | YAML設定ファイル |
| `markdown` | Markdownの記法例 |
| `text` | 出力例・ディレクトリ構成など言語に該当しないもの |
| `csv` | CSV形式データ |
| `sql` | SQLクエリ |
| `bibtex` | BibTeX文献情報 |

### 記法の統一ルール

- コードフェンス記号はバッククォート（`` ` ``）に統一する（`~~~` は使用しない）
- 強調は `*斜体*`、太字は `**太字**` に統一する（`_斜体_`、`__太字__` は使用しない）
- 見出しは `#`（ATXスタイル）に統一する
- リストマーカーはファイル内で統一する（`-` を推奨）
- ドキュメント本文にIssue番号を記載しない（対象・例外は [4.1](#41-issue番号の記載禁止コードドキュメント共通) を参照）

### markdownlint で無効化しているルール

| ルール | 理由 |
|---|---|
| MD013（行の長さ制限） | 日本語テキスト・テーブルでは行長制限が実用的でないため |
| MD025（単一H1） | アーカイブ文献要約など `#` で大セクションを区切る構造を許容 |
| MD028（引用ブロック内空行） | 独立した引用を空行で区切る記法を許容 |
| MD036（強調の見出し利用禁止） | `**①手順名**` や `**【カテゴリ名】**` は意図的な強調であり見出しではない |
| MD060（テーブルスタイル） | パイプ周囲のスペース強制は日本語テーブルで非実用的 |

### markdownlint でカスタム設定しているルール

| ルール | 設定内容 | 理由 |
|---|---|---|
| MD024（見出し重複禁止） | 同一セクション内のみ禁止 | 異なるセクションでの同名見出し（「概要」等）を許容 |
| MD033（インラインHTML禁止） | `<br>`, `<sup>`, `<sub>`, `<details>`, `<summary>` を許可 | テーブル内改行・脚注で使用するため |

## 15. 実装前後チェックリスト（必須）

以下は、AI実装・手動実装のどちらでも**コミット前に必ず確認**する。

### 15.1 実装前チェック

- [ ] 関連ドキュメント（少なくとも `docs/02_methods/CodingRule.md` と対象手法仕様）を再確認した
- [ ] 入出力パスは相対パスで設計し、`pathlib.Path` を使う方針にした
- [ ] 関数責務を分割し、1関数1責務の構成を決めた
- [ ] テスト方針を決めた（対象関数・テストの種類・テストデータ）
- [ ] 共通モジュール（`src/common/`）に同等機能がないか確認した

### 15.2 実装後チェック

- [ ] `ruff check` と `ruff format` を実行し、エラー・差分がないことを確認した
- [ ] 変数名・関数名・定数名が命名規則に従っている（日本語変数名を使っていない）
- [ ] 追加・変更した関数に型ヒントと日本語docstringを付与した
- [ ] 複雑な処理に日本語コメントを付与した（過不足なく簡潔に）
- [ ] 例外処理を実装し、エラーメッセージが日本語で理解しやすい
- [ ] 再現性に影響する設定（パス、乱数、閾値）を明示した
- [ ] 不要なコード・未使用変数・デバッグ出力を残していない
- [ ] テストコードを作成し、`pytest` で通過することを確認した
- [ ] 既存共通機能と重複する実装を新たに作っていない

### 15.3 出力前チェック

- [ ] 変更ファイルを自分で読み返し、規約違反がないことを確認した
- [ ] 主要ファイルの静的エラー（LSP/構文エラー）を確認した
- [ ] 変更内容を説明する際、根拠となるファイルを明示できる状態にした
- [ ] ドキュメントを追加・改名した場合は `docs/README.md` を更新した
- [ ] `environment.yml` を更新した場合は `docs/setup.md` も確認した
- [ ] `.gitignore` や生成物方針を更新した場合は `docs/02_methods/data_management_guide.md` も確認した

## 16. ツール利用時の読み取り規律

AIエージェント（Claude Code等）がファイル読み取り・シェル実行を行う際、同一情報の重複取得や出力の垂れ流しはコンテキストを無駄に消費する。以下をタスク実行時の運用規律とする。

- 同一セッション内で同じファイルを読み直さない（Edit後の再Readは不要。ハーネスがファイル状態を追跡している）
- 1,000行を超えるファイルは、まずGrepで該当箇所を特定してからReadの`offset`・`limit`で部分読みする
- Bashでの `grep` / `cat` / `ls` / `find` は、専用ツール（Grep / Glob / Read）に置き換える（`head_limit`・`output_mode`で出力を絞れる）
- シェル出力は必ず絞る（例: `pytest -q`、`git log --oneline -n <件数>`、`gh ... --json <フィールド> --jq <式>`、`| head`）
- 作業ディレクトリは設定済みのため、コマンドに `cd` を前置しない
- 依存関係のない独立したツール呼び出しは、可能な限り1メッセージにまとめる

**適用対象外**: 本規律は同一情報の重複取得・出力の垂れ流しの抑制のみを対象とする。必要な確認・テスト・レビューの範囲を狭めるものではない（実装前後チェックリストや検証観点を省略する根拠にしない）。

---

この規約に従い、研究プロジェクトの品質向上と円滑な共同作業を目指してください。
