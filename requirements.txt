earthengine-api
geopandas
fiona
# 属性のみのGeoPackage書き出しと列を絞った読み出しに直接使う。environment.yml は
# 再現性のため 0.11 をピンしており、こちらは必要な下限のみを示す。
pyogrio>=0.11
pyproj
shapely
pandas
numpy
tqdm
rasterio
h5py
requests
scipy>=1.9
scikit-learn
# Axes.set_xticks(labels=...) は3.5.0以降で使用可能なため下限を明示する
matplotlib>=3.5
# 動作確認済みバージョンを下限として明示する
shap>=0.48
