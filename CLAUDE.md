# Claude Code Instructions - 修士研究プロジェクト

> **このファイルの役割**  
> Claude Codeがすべての会話で自動的に読み込むプロジェクト固有の指示です。  
> 常時必要な原則・用語のみをここに置き、詳細手順は参照先ドキュメント・カスタムコマンド・スキルに委ねます。

---

## 研究概要

### 研究テーマ

ベトナム主要都市を対象とした地表面温度と都市構造の関係性評価に関する研究

### 研究目的

都市を構成する物理的要素が地表面温度（LST）分布に与える影響を定量的に明らかにし、ヒートアイランド現象の発生要因を空間的に把握する。また、途上国大都市におけるデータ制約下での分析手法の有効性を検証する。

### 主要なResearch Questions（RQ）

- **RQ1**: Landsat 8（30m）LSTと都市構造データを組み合わせた場合、どの説明変数がLSTに対して支配的か？
- **RQ2**: 都市構造パラメータとLSTの関係性は、空間集計単位や解析スケールの違いによってどのように変化するか？
- **RQ3**: 測量データが限定的な条件下でも、衛星データおよび公開データによりLST分布の説明はどの程度可能か？

**詳細**: [research_guide.md](docs/01_planning/research_guide.md)

---

## 最小用語集

- **LST**: 地表面温度。**本研究では必ず摂氏（°C）で扱う**
- **ROI**: 研究対象地域。分析対象域の基準となる空間範囲
- **CRS**: 原則として入出力は **WGS84（EPSG:4326）** を基準とする。面積・距離計算時は投影座標系を使用する。ベトナム測量データは **VN-2000** を含む
- **Satellite Only**: 衛星由来指標のみを用いる分析シナリオ
- **Limited**: 衛星データと公開GISのみを用いる分析シナリオ
- **Full**: 衛星データ・公開GIS・測量GISを用いる分析シナリオ
- **有効カバレッジ / 有効域**: 実際に信頼して使えるデータ範囲。**ROI全体と同一視しない**

---

## コーディング規約（要約）

- **PEP 8準拠**: スペース4つインデント、タブ禁止
- **日本語コメント・docstring必須**（日本語変数名は禁止）
- **命名規則**: 変数・関数はスネークケース、クラスはキャメルケース
- **1関数1責務**、再現性の確保（相対パス・`pathlib.Path`・乱数シード設定）
- Pythonファイルの新規作成・機能変更時はテストコードを作成し `pytest` 通過を確認する
- テスト方針・実装前後チェックリストを含む**詳細**: [CodingRule.md](docs/02_methods/CodingRule.md)を必ず参照

---

## タスクワークフロー

- **GitHub Issues がタスク管理の正本**。起票は `/issue-create` スキルを使用する（優先度確認を含む）
- **タスク着手は `/task-start {Issue番号}`**（状態確認・単一/並列判定・要件確認・ブランチ作成・計画承認を含む）
- **マージ後処理は `/task-done`**、CodeRabbit 対応は `/coderabbit {PR番号}`
- **承認ゲート**: ① 着手前の計画承認 ② コミット単位のレビュー承認（段階的コミット） ③ 完了時のPRレビュー（完了条件とPR本文を一括確認）
- **段階的コミット**: 実装 → lint → レビュー提示 → 承認 → コミット のサイクル。軽微な修正（typo・lint対応等）は複数まとめて提示可。**Tier 1 並列タスクのみ中間承認を免除**
- **push の条件**: (a) タスク完了フロー内のPR作成時 (b) ユーザーの明示指示 — の2条件のみ
- **セッションを開始しただけでは外部状態を変更しない**（Issue・Project・リモートへの書き込みは着手指示後）
- **1 Issue = 1 PR** で完結する変更単位を目安とし、並列タスクは worktree で隔離して実行する
- **詳細**: [task-workflow.md](.github/task-workflow.md) / 並列実行: [parallel-workflow.md](.github/parallel-workflow.md)

---

## タスク実行時の共通ルール

### 必須事項

1. タスク実行前に関連ファイル・`docs/` 配下の関連ドキュメントを確認する
2. コード変更時は [CodingRule.md](docs/02_methods/CodingRule.md) の規約・テスト方針・「15. 実装前後チェックリスト」に従う
3. 長時間・大規模処理では中間結果の保存・進捗表示・ログ出力を行う

### 禁止事項

- 研究データの絶対パスのハードコード（例外条件は CodingRule.md 7章）
- スキル（`.claude/skills/`）の作成・変更を**ユーザーの明示承認なしに**行うこと（**詳細**: [skill_operation_rules.md](docs/02_methods/skill_operation_rules.md)）

### 機械的ガードレール

強制push・`git reset --hard`・`gh pr merge`（squash mergeはユーザーの専権）等の危険操作は `.claude/settings.json` の `deny` で禁止している。**deny が正**であり、ドキュメント中の記述はその補足である。

---

## AI支援の最小方針

- **期待すること**: コード補助（Python / GIS処理）、分析補助（手順整理・検証観点の明確化）、文書補助（結果整理・要約・構成改善）
- **出力の条件**: 推測で埋めず、関連ファイル・関連ドキュメント・実行結果を根拠にする。未確認事項は断定せず、仮定または要確認事項として明示する
- **研究者の責任範囲**: 学術的判断、結論、研究倫理に関わる最終判断は研究者自身が負う

---

## ドキュメント管理のルール

- **`docs/README.md` が唯一の真実の情報源（Single Source of Truth）**: 全ドキュメントの構造・概要はここに集約する。サブREADMEは作成禁止（例外: `docs/04_archive/README.md`）。同じ内容を複数ファイルに記載しない
- **新規ドキュメント作成時**: 適切なフェーズフォルダ（`01_planning/` 研究計画、`02_methods/` 手法、`03_results/` 結果、`04_archive/` 参考資料）に配置し、`docs/README.md` のカタログ・詳細・変更履歴を必ず更新する。冒頭にメタ情報（最終更新・関連ドキュメント・前提知識）を記載し、相互参照リンクを設定する
- **移動・削除時**: リンク切れの影響範囲を確認してから実施し、`docs/README.md` のパスを更新する。Git履歴を保持する（`git mv` を使用）

---

## このファイルの更新方針

- 研究の進行に応じて随時更新し、変更時は日付を記録する
- 詳細手順はここに書かず、参照先（task-workflow.md・CodingRule.md・コマンド・スキル）に置く

**最終更新**: 2026-07-08
